// ⚙️ GENERATED BY TRISHUL — DO NOT EDIT

import * as productService from './product.service.js';

/**
 * GET /products
 * @output {{"products":"string"}}
 */
export async function listProducts(req, res, next) {
  try {
    const result = await productService.listProducts();
    res.json(result);
  } catch (error) {
    next(error);
  }
}

/**
 * POST /products
 * @input {{"name":"string","price":"number","description":"string"}}
 * @output {{"id":"string","name":"string","price":"number"}}
 */
export async function createProduct(req, res, next) {
  try {
    const { name, price, description } = req.body;
    const result = await productService.createProduct({ name, price, description });
    res.json(result);
  } catch (error) {
    next(error);
  }
}

/**
 * PUT /products/:id
 * @input {{"name":"string","price":"number","description":"string"}}
 * @output {{"id":"string","name":"string","price":"number"}}
 */
export async function updateProduct(req, res, next) {
  try {
    const { name, price, description } = req.body;
    const { id } = req.params;
    const result = await productService.updateProduct({ id, name, price, description });
    res.json(result);
  } catch (error) {
    next(error);
  }
}

/**
 * DELETE /products/:id
 * @output {{"success":"boolean"}}
 */
export async function deleteProduct(req, res, next) {
  try {
    const { id } = req.params;
    const result = await productService.deleteProduct({ id });
    res.json(result);
  } catch (error) {
    next(error);
  }
}

